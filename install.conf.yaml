- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/gitconfig
    ~/.xinitrc: xorg/xinitrc

- create:
    - ~/testing/test/test

# - shell:
